1. GUI 프로그래밍,이벤트프로그래밍 = 옵저버패턴
   - 스윙 (리스너)
   - 자바스크립트 (사용자이벤트,브라우저이벤트)
   - jquery (커스텀이벤트,약칭이벤트,trigger,on 등)
   - Node.js (EventEmitter)

2. MVC 패턴 과 연계
   - 모델이 바뀌면 뷰를 갱신한다 (카카오톡 pc 버전 대화방이름을 바꾸면 모바일 에서도 변경됨)
   - Backbone.js, Angular.js 등

3. 푸쉬서버
   - GCM, 아이폰

4. 채팅
   - 브로드 캐스트
   - 카카오톡

5. xml파서
   - sax 파서

6. AOP 에서도 사용할 수 있음


$(document).on('click',function() {
});



$().click(function() {

});